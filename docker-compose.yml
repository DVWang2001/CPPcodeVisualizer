version: '3.8'
services:
  npm-env:
    image: node:16 # Node.js 16 comes with npm 8.5.1
    volumes:
      - ./:/app
    working_dir: /app
    command: sh -c "npm install && npm start"
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development

  gdbgui:
    build:
      context: .
      dockerfile: Dockerfile.gdbgui
    volumes:
      - ./:/app
      - ./gdbgui/server/uploads:/app/gdbgui/server/uploads
    working_dir: /app
    command: ["/app/gdbgui-entry.sh"]
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1

