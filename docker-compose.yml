version: '3.8'
services:
  gdbgui:
    build:
      context: .
      dockerfile: Dockerfile.gdbgui
    ports:
      - "5000:5000"
    volumes:
      - ./gdbgui/server/uploads:/app/gdbgui/server/uploads
    environment:
      - PYTHONUNBUFFERED=1
